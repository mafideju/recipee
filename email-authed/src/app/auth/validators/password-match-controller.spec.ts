import { PasswordMatchController } from './password-match-controller';

describe('PasswordMatchController', () => {
  it('should create an instance', () => {
    expect(new PasswordMatchController()).toBeTruthy();
  });
});
